
import Foundation

import UIKit

final class MMO_ModityViewsController: UIViewController {
private var srcIdleUsersStr: String?
private var call_max: Double? = 0.0
var cellMediaString: String!
private var pop_margin: Float? = 0.0




    private var team: MMO_Bootstrap
    private let bgView = UIImageView(image: UIImage(named: "team_bg") ?? UIImage(named: "home_bg"))
    private let header = MMO_EditHeader()
    private let scrollView = UIScrollView()
    private let contentView = UIView()

    private let avatarView = UIImageView()
    private let nameLabel = UILabel()
    private let dateLabel = UILabel()
    private let card = UIView()
    private let routeTitleLabel = UILabel()
    private let routeValueLabel = UILabel()
    private let descTitleLabel = UILabel()
    private let descValueLabel = UILabel()
    private let membersStack = UIStackView()
    private let joinButton = UIButton(type: .custom)
    private let coinIcon = UIImageView(image: UIImage(named: "team_coin"))

    init(team: MMO_Bootstrap) {
        self.team = team
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) { fatalError() }

@discardableResult
 func addFieldViewBarTableView() -> UITableView! {
    var fullM: String! = String(cString: [114,97,105,116,0], encoding: .utf8)!
    var guide4: String! = String(cString: [99,115,114,105,100,0], encoding: .utf8)!
   while (fullM == String(cString:[80,0], encoding: .utf8)!) {
      guide4 = "\(fullM.count)"
      break
   }
      guide4 = "\(guide4.count ^ 1)"
     var clubsTitles: [Any]! = [72, 86, 14]
     var removingHas: UIButton! = UIButton(frame:CGRect(x: 245, y: 259, width: 0, height: 0))
    var sourcesTiers = UITableView()
    removingHas.frame = CGRect(x: 272, y: 246, width: 0, height: 0)
    removingHas.alpha = 0.9;
    removingHas.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    sourcesTiers.frame = CGRect(x: 6, y: 175, width: 0, height: 0)
    sourcesTiers.alpha = 0.5;
    sourcesTiers.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sourcesTiers.delegate = nil
    sourcesTiers.dataSource = nil
    sourcesTiers.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return sourcesTiers

}






    @objc private func joinTap() {

         let transformersMinimum: UITableView! = addFieldViewBarTableView()

      if transformersMinimum != nil {
          self.view.addSubview(transformersMinimum)
          let transformersMinimum_tag = transformersMinimum.tag
      }
      else {
          print("transformersMinimum is nil")      }

_ = transformersMinimum


       var arrowm: Double = 1.0
       var observerf: [String: Any]! = [String(cString: [108,101,114,112,0], encoding: .utf8)!:String(cString: [114,101,115,111,108,118,101,114,115,0], encoding: .utf8)!, String(cString: [112,117,98,105,99,0], encoding: .utf8)!:String(cString: [115,116,97,114,116,0], encoding: .utf8)!, String(cString: [111,112,112,111,114,116,117,110,105,115,116,105,99,97,108,108,121,0], encoding: .utf8)!:String(cString: [115,105,103,116,101,114,109,0], encoding: .utf8)!]
       var photoy: Float = 1.0
       var officiali: String! = String(cString: [98,97,115,101,110,97,109,101,0], encoding: .utf8)!
       _ = officiali
         photoy -= (Float(Int(photoy > 258103127.0 || photoy < -258103127.0 ? 20.0 : photoy) + 3))
      while ((officiali.count >> (Swift.min(labs(4), 5))) > 2 || 4 > (observerf.keys.count >> (Swift.min(officiali.count, 3)))) {
          var controlZ: [Any]! = [[String(cString: [115,99,114,117,98,98,101,114,0], encoding: .utf8)!, String(cString: [99,111,114,100,0], encoding: .utf8)!, String(cString: [100,116,100,102,0], encoding: .utf8)!]]
          _ = controlZ
          var kind3: Bool = true
          var onewsh: Bool = false
          _ = onewsh
         officiali = "\(((onewsh ? 3 : 4) >> (Swift.min(labs(Int(photoy > 287662641.0 || photoy < -287662641.0 ? 12.0 : photoy)), 4))))"
         controlZ.append((1 - (kind3 ? 5 : 2)))
         kind3 = photoy == 76.88 && observerf.count == 60
         break
      }
         officiali = "\(2 / (Swift.max(5, officiali.count)))"
       var open9: Float = 0.0
       var headera: Float = 0.0
      if 2.73 == (Double(Int(photoy) * 1)) {
         headera -= (Float(Int(photoy > 285022267.0 || photoy < -285022267.0 ? 67.0 : photoy)))
      }
      while (3.85 < photoy) {
          var targetd: Float = 0.0
          var selectede: String! = String(cString: [115,117,98,116,114,97,99,116,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &selectede) { pointer in
                _ = pointer.pointee
         }
         photoy *= (Float(selectede.count / (Swift.max(4, Int(open9 > 265232093.0 || open9 < -265232093.0 ? 41.0 : open9)))))
         targetd -= (Float(2 | Int(photoy > 242961571.0 || photoy < -242961571.0 ? 9.0 : photoy)))
         break
      }
      repeat {
          var controlh: [String: Any]! = [String(cString: [98,108,97,110,107,0], encoding: .utf8)!:58, String(cString: [98,115,119,97,112,100,115,112,0], encoding: .utf8)!:55]
         withUnsafeMutablePointer(to: &controlh) { pointer in
                _ = pointer.pointee
         }
          var playw: String! = String(cString: [110,118,100,101,99,0], encoding: .utf8)!
          var components5: Double = 4.0
          var modeO: Double = 3.0
         withUnsafeMutablePointer(to: &modeO) { pointer in
                _ = pointer.pointee
         }
          var removek: Bool = true
          _ = removek
         photoy += Float(observerf.count)
         controlh = ["\(controlh.keys.count)": controlh.count >> (Swift.min(labs(1), 5))]
         playw = "\(((String(cString:[65,0], encoding: .utf8)!) == officiali ? officiali.count : Int(photoy > 331718346.0 || photoy < -331718346.0 ? 53.0 : photoy)))"
         components5 += Double(2)
         modeO *= (Double((String(cString:[109,0], encoding: .utf8)!) == officiali ? officiali.count : Int(open9 > 302339868.0 || open9 < -302339868.0 ? 35.0 : open9)))
         removek = 33.47 < headera
         if photoy == 2269961.0 {
            break
         }
      } while (photoy == 2269961.0) && (Float(officiali.count) <= photoy)
      if 2 == officiali.count {
          var label5: Bool = true
          var agej: String! = String(cString: [115,115,105,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &agej) { pointer in
    
         }
          var messages4: String! = String(cString: [105,110,116,101,114,112,111,108,97,116,101,112,111,108,121,100,101,99,0], encoding: .utf8)!
          var stampr: String! = String(cString: [115,97,102,97,114,121,0], encoding: .utf8)!
          _ = stampr
          var statsg: [String: Any]! = [String(cString: [116,120,116,111,98,106,0], encoding: .utf8)!:2, String(cString: [103,114,111,119,0], encoding: .utf8)!:55, String(cString: [106,105,103,103,108,101,0], encoding: .utf8)!:57]
         photoy += Float(1)
         label5 = ((agej.count / (Swift.max(3, Int(headera > 334908830.0 || headera < -334908830.0 ? 32.0 : headera)))) == 46)
         agej.append("\(stampr.count / 1)")
         messages4 = "\(agej.count)"
         stampr = "\(3 + messages4.count)"
         statsg["\(label5)"] = 1 ^ statsg.values.count
      }
         officiali = "\(observerf.keys.count)"
      arrowm -= (Double(1 - Int(photoy > 31377936.0 || photoy < -31377936.0 ? 77.0 : photoy)))

        guard !team.joined else { return }
        MMO_MotoLaunch.shared.joinTeam(team.id) { [weak self] result in
            guard let self else { return }
            switch result {
            case .success:
                self.refreshJoinState()
                MMO_RouterAuthView.show(on: self.view, title: "Joined", message: "You joined this ride group. 10 coins used.", actions: [("OK", true, {})])
            case .failure:
                MMO_RouterAuthView.show(on: self.view, title: "Not enough coins", message: "Joining a ride costs 10 coins. Top up your wallet to continue.", actions: [
                    ("Go Wallet", true, { [weak self] in
                        self?.navigationController?.pushViewController(MMO_CenterViewsController(), animated: true)
                    }),
                    ("Cancel", false, {})
                ])
            }
        }
    }

@discardableResult
 func launchManagerAny(tabRing: Int, videoAge: Double) -> Bool {
    var nickname3: Double = 1.0
    var buttonsn: String! = String(cString: [109,110,99,0], encoding: .utf8)!
    var trackingj: Bool = false
       var defaults_: String! = String(cString: [115,119,105,116,99,104,0], encoding: .utf8)!
       var shouldT: Int = 1
       var short_un: String! = String(cString: [122,114,101,111,114,100,101,114,0], encoding: .utf8)!
       _ = short_un
         shouldT += shouldT
         shouldT /= Swift.max(5, defaults_.count & shouldT)
         short_un = "\(2 >> (Swift.min(2, labs(shouldT))))"
         shouldT |= shouldT
      while (3 < (shouldT % (Swift.max(1, defaults_.count))) || 3 < (shouldT % (Swift.max(6, defaults_.count)))) {
          var containerK: String! = String(cString: [110,111,116,101,115,0], encoding: .utf8)!
          _ = containerK
          var speedw: String! = String(cString: [110,111,110,99,101,0], encoding: .utf8)!
         shouldT >>= Swift.min(3, short_un.count)
         containerK = "\(3)"
         speedw.append("\(speedw.count)")
         break
      }
      for _ in 0 ..< 3 {
          var fieldO: Bool = false
          var emptyU: Double = 4.0
          _ = emptyU
          var backgroundP: String! = String(cString: [117,110,105,102,111,114,109,0], encoding: .utf8)!
          var credentialK: Double = 3.0
          var validh: [Any]! = [84, 11]
         shouldT -= shouldT ^ 1
         fieldO = defaults_.count == 23
         emptyU /= Swift.max(Double(1 / (Swift.max(4, backgroundP.count))), 3)
         backgroundP.append("\(3)")
         credentialK -= (Double(Int(emptyU > 238609558.0 || emptyU < -238609558.0 ? 5.0 : emptyU)))
         validh.append(short_un.count | 2)
      }
          var modev: Double = 0.0
          var dotv: Double = 5.0
          var providerk: String! = String(cString: [119,111,114,115,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &providerk) { pointer in
    
         }
         short_un.append("\(providerk.count + 2)")
         modev += Double(short_un.count)
         dotv -= Double(3)
       var accuracyU: Bool = false
       _ = accuracyU
       var dir5: Bool = true
       _ = dir5
         accuracyU = !short_un.hasSuffix("\(accuracyU)")
         dir5 = !accuracyU
      nickname3 /= Swift.max(1, Double(defaults_.count * 3))
   repeat {
      trackingj = trackingj || nickname3 < 56.30
      if trackingj ? !trackingj : trackingj {
         break
      }
   } while (trackingj ? !trackingj : trackingj) && (!trackingj)
       var likedU: String! = String(cString: [120,114,117,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &likedU) { pointer in
    
      }
       var closeq: Bool = false
       var key8: [Any]! = [String(cString: [97,112,112,114,111,120,105,109,97,116,101,0], encoding: .utf8)!, String(cString: [110,111,116,105,102,121,0], encoding: .utf8)!]
         key8.append(((closeq ? 5 : 2) - 2))
      for _ in 0 ..< 3 {
         likedU.append("\((1 % (Swift.max(2, (closeq ? 4 : 1)))))")
      }
         closeq = !closeq
      for _ in 0 ..< 2 {
         key8 = [likedU.count]
      }
      for _ in 0 ..< 2 {
         likedU.append("\((1 - (closeq ? 3 : 3)))")
      }
          var texts9: String! = String(cString: [98,105,111,0], encoding: .utf8)!
         key8.append(3)
         texts9.append("\(((String(cString:[83,0], encoding: .utf8)!) == texts9 ? (closeq ? 2 : 3) : texts9.count))")
       var borderw: String! = String(cString: [116,119,111,112,97,115,115,0], encoding: .utf8)!
       _ = borderw
      if 3 == (borderw.count - 1) && (borderw.count - 1) == 3 {
         key8.append(likedU.count)
      }
         key8 = [key8.count]
      buttonsn = "\(1)"
   return trackingj

}






    override func viewWillAppear(_ animated: Bool) {

         let renderInterlaced: Bool = launchManagerAny(tabRing:36, videoAge:20.0)

      if renderInterlaced {
          print("ok")
      }

_ = renderInterlaced


       var bioE: Int = 3
   if 5 <= (1 & bioE) || (1 & bioE) <= 4 {
       var screenk: Double = 5.0
       var commentj: Float = 3.0
      withUnsafeMutablePointer(to: &commentj) { pointer in
             _ = pointer.pointee
      }
       var startedy: Bool = true
       var scrollU: String! = String(cString: [116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!
         commentj -= (Float(Int(screenk > 41093056.0 || screenk < -41093056.0 ? 19.0 : screenk)))
      repeat {
         startedy = commentj > 36.40
         if startedy ? !startedy : startedy {
            break
         }
      } while (startedy ? !startedy : startedy) && (!startedy)
          var hostsw: [String: Any]! = [String(cString: [117,100,105,111,0], encoding: .utf8)!:17, String(cString: [105,109,112,111,114,116,97,98,108,101,0], encoding: .utf8)!:40]
          var starred2: Bool = true
         scrollU.append("\(1)")
         hostsw = ["\(commentj)": scrollU.count / 2]
         starred2 = scrollU.count == 62
       var lengthC: [Any]! = [41, 8]
       var valueW: [Any]! = [86, 66]
      withUnsafeMutablePointer(to: &valueW) { pointer in
    
      }
       var sendW: String! = String(cString: [115,116,97,116,101,102,117,108,0], encoding: .utf8)!
          var clubsk: String! = String(cString: [100,105,114,110,97,109,101,0], encoding: .utf8)!
          var inkB: Double = 1.0
          var iconL: [String: Any]! = [String(cString: [100,101,118,105,100,101,0], encoding: .utf8)!:17, String(cString: [117,110,105,110,115,116,97,108,108,0], encoding: .utf8)!:17]
         sendW.append("\(((startedy ? 3 : 4) | sendW.count))")
         clubsk.append("\(((startedy ? 4 : 1) / (Swift.max(Int(screenk > 346820714.0 || screenk < -346820714.0 ? 40.0 : screenk), 7))))")
         inkB -= Double(3 >> (Swift.min(2, clubsk.count)))
         iconL["\(inkB)"] = (Int(inkB > 99335466.0 || inkB < -99335466.0 ? 91.0 : inkB))
      if screenk <= 1.49 {
          var authO: String! = String(cString: [105,110,118,105,116,97,116,105,111,110,0], encoding: .utf8)!
          var visibleU: String! = String(cString: [112,99,109,98,0], encoding: .utf8)!
         startedy = (sendW.count << (Swift.min(4, valueW.count))) >= 38
         authO.append("\(authO.count)")
         visibleU.append("\(valueW.count)")
      }
      if 1 >= (sendW.count - lengthC.count) {
          var device4: String! = String(cString: [116,114,97,110,115,108,97,116,111,110,0], encoding: .utf8)!
          var teamc: String! = String(cString: [112,111,110,103,0], encoding: .utf8)!
          var mediar: Bool = false
         withUnsafeMutablePointer(to: &mediar) { pointer in
    
         }
         sendW = "\(2 * lengthC.count)"
         device4.append("\((Int(commentj > 264400227.0 || commentj < -264400227.0 ? 71.0 : commentj) * 1))")
         teamc = "\(1)"
         mediar = 10 <= valueW.count
      }
       var k_countl: String! = String(cString: [100,101,109,117,120,101,114,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &k_countl) { pointer in
             _ = pointer.pointee
      }
          var passwordm: String! = String(cString: [103,101,116,102,114,97,109,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &passwordm) { pointer in
    
         }
          var imgg: Float = 4.0
          _ = imgg
          var alerth: String! = String(cString: [112,97,114,116,121,0], encoding: .utf8)!
         k_countl = "\((Int(imgg > 5897751.0 || imgg < -5897751.0 ? 48.0 : imgg)))"
         passwordm.append("\((Int(imgg > 362747598.0 || imgg < -362747598.0 ? 95.0 : imgg)))")
         alerth.append("\(lengthC.count)")
          var claudeF: Double = 4.0
          _ = claudeF
          var crypt5: [String: Any]! = [String(cString: [97,114,99,104,105,118,101,0], encoding: .utf8)!:86, String(cString: [112,114,111,112,111,114,116,105,111,110,0], encoding: .utf8)!:82]
          var stylew: String! = String(cString: [115,113,108,105,116,101,115,101,115,115,105,111,110,0], encoding: .utf8)!
         commentj /= Swift.max(Float(k_countl.count), 3)
         claudeF -= (Double((startedy ? 1 : 3) ^ 2))
         crypt5 = ["\(crypt5.values.count)": crypt5.values.count]
         stylew = "\(valueW.count << (Swift.min(labs(1), 1)))"
      for _ in 0 ..< 2 {
          var compactS: Bool = false
          var teamO: Bool = false
          var msgd: String! = String(cString: [98,105,110,100,105,110,103,115,0], encoding: .utf8)!
          var avatarv: String! = String(cString: [103,111,111,100,0], encoding: .utf8)!
          var pushr: String! = String(cString: [100,105,97,103,114,97,109,0], encoding: .utf8)!
          _ = pushr
         k_countl.append("\(((startedy ? 4 : 4) ^ 1))")
         compactS = screenk >= Double(commentj)
         msgd.append("\(valueW.count / 2)")
         avatarv = "\(((teamO ? 4 : 3) + 3))"
         pushr = "\(pushr.count)"
      }
      bioE -= scrollU.count
   }

        super.viewWillAppear(animated)
        refreshJoinState()
    }

@discardableResult
 func restoreFirstClientModeButton(overlapKeyboard: String!) -> UIButton! {
    var medals5: String! = String(cString: [115,109,111,111,116,104,101,110,0], encoding: .utf8)!
    var postsB: Bool = false
   withUnsafeMutablePointer(to: &postsB) { pointer in
    
   }
   for _ in 0 ..< 3 {
       var countries8: Double = 2.0
       var overlay5: Bool = false
       var v_imageK: [String: Any]! = [String(cString: [109,97,115,107,105,110,103,0], encoding: .utf8)!:String(cString: [115,121,115,108,111,103,0], encoding: .utf8)!]
      for _ in 0 ..< 2 {
         overlay5 = v_imageK.keys.count == 93
      }
          var generatorb: Bool = true
          var left8: String! = String(cString: [115,116,115,115,0], encoding: .utf8)!
         countries8 /= Swift.max((Double(Int(countries8 > 88403887.0 || countries8 < -88403887.0 ? 5.0 : countries8))), 2)
         generatorb = v_imageK.count > 50
         left8 = "\(1 | left8.count)"
      while (!overlay5) {
          var loads: Int = 0
          var header3: Double = 4.0
          var reportsA: Int = 2
          _ = reportsA
          var closeu: [String: Any]! = [String(cString: [110,105,98,98,108,101,0], encoding: .utf8)!:String(cString: [100,101,102,97,117,108,116,99,111,101,102,99,111,117,110,116,115,0], encoding: .utf8)!]
         countries8 += Double(1)
         loads /= Swift.max(3, closeu.values.count ^ 2)
         header3 -= (Double(reportsA - Int(header3 > 10348320.0 || header3 < -10348320.0 ? 53.0 : header3)))
         reportsA -= 3 - reportsA
         closeu = ["\(header3)": (Int(header3 > 106010622.0 || header3 < -106010622.0 ? 32.0 : header3) | (overlay5 ? 5 : 1))]
         break
      }
      while (1.3 == countries8) {
          var agey: [Any]! = [7, 50, 74]
         countries8 += Double(1 * agey.count)
         break
      }
      if (v_imageK.keys.count - 3) >= 5 && (v_imageK.keys.count * Int(countries8 > 100577995.0 || countries8 < -100577995.0 ? 13.0 : countries8)) >= 3 {
         countries8 -= Double(v_imageK.count)
      }
          var removedl: Double = 4.0
          var update_sig: Double = 0.0
         v_imageK["\(countries8)"] = (Int(update_sig > 191580799.0 || update_sig < -191580799.0 ? 26.0 : update_sig) * 3)
         removedl += (Double(Int(update_sig > 382765696.0 || update_sig < -382765696.0 ? 85.0 : update_sig) << (Swift.min(2, labs((overlay5 ? 4 : 5))))))
      repeat {
          var views2: String! = String(cString: [98,114,105,101,102,108,121,0], encoding: .utf8)!
          var rawv: Int = 5
         withUnsafeMutablePointer(to: &rawv) { pointer in
    
         }
          var controlz: String! = String(cString: [112,114,101,108,111,97,100,101,100,0], encoding: .utf8)!
          var outS: String! = String(cString: [100,101,105,110,116,101,114,108,101,97,118,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &outS) { pointer in
    
         }
          var bikeN: String! = String(cString: [100,111,109,97,105,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &bikeN) { pointer in
                _ = pointer.pointee
         }
         overlay5 = v_imageK.keys.count > 65 || !overlay5
         views2.append("\((bikeN == (String(cString:[87,0], encoding: .utf8)!) ? bikeN.count : Int(countries8 > 159465404.0 || countries8 < -159465404.0 ? 26.0 : countries8)))")
         rawv ^= (Int(countries8 > 129037190.0 || countries8 < -129037190.0 ? 51.0 : countries8))
         controlz.append("\(3)")
         outS = "\((1 + Int(countries8 > 390061577.0 || countries8 < -390061577.0 ? 76.0 : countries8)))"
         if overlay5 ? !overlay5 : overlay5 {
            break
         }
      } while (overlay5) && (overlay5 ? !overlay5 : overlay5)
         countries8 -= (Double(3 & Int(countries8 > 63195625.0 || countries8 < -63195625.0 ? 61.0 : countries8)))
          var chatd: Float = 2.0
          var frame_4aR: [Any]! = [UILabel(frame:CGRect.zero)]
          _ = frame_4aR
          var read0: Double = 5.0
         v_imageK["\(read0)"] = 2
         chatd += (Float(2 | (overlay5 ? 2 : 4)))
         frame_4aR = [(Int(chatd > 123287624.0 || chatd < -123287624.0 ? 15.0 : chatd))]
      postsB = 87.63 < countries8 || medals5 == (String(cString:[72,0], encoding: .utf8)!)
   }
       var statusX: String! = String(cString: [115,99,114,111,108,108,101,114,0], encoding: .utf8)!
       var meta0: Double = 0.0
       var biked: [Any]! = [26, 48, 88]
          var buttonsh: Int = 5
         biked = [2]
         buttonsh -= 2 ^ buttonsh
         meta0 -= Double(biked.count)
      postsB = !statusX.contains("\(postsB)")
     var backgroundNow: Bool = true
     var refreshLatest: [Any]! = [String(cString: [110,117,109,101,114,105,102,121,0], encoding: .utf8)!]
     let feedShows: [Any]! = [96, 7]
     let controllerMore: Double = 59.0
    var submitAccessiblity = UIButton()
    submitAccessiblity.frame = CGRect(x: 301, y: 82, width: 0, height: 0)
    submitAccessiblity.alpha = 0.3;
    submitAccessiblity.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return submitAccessiblity

}






    private func bindData() {

         let precenceDnxhdenc: UIButton! = restoreFirstClientModeButton(overlapKeyboard:String(cString: [114,101,108,101,97,115,101,0], encoding: .utf8)!)

      if precenceDnxhdenc != nil {
          self.view.addSubview(precenceDnxhdenc)
          let precenceDnxhdenc_tag = precenceDnxhdenc.tag
      }
      else {
          print("precenceDnxhdenc is nil")      }

_ = precenceDnxhdenc


       var observerK: Bool = false
   while (!observerK) {
      observerK = !observerK && observerK
      break
   }

        let return_iw = CatalogBootstrap.user(id: team.hostId)
        avatarView.image = UIImage(named: return_iw?.avatarName ?? team.coverName)
        nameLabel.text = return_iw?.nickname ?? "Rider"
        dateLabel.text = routeDateText()
        routeValueLabel.text = team.title
        descValueLabel.text = team.detail

        membersStack.arrangedSubviews.forEach { $0.removeFromSuperview() }
        for name in team.memberAvatarNames.prefix(4) {
            let imgView = UIImageView(image: UIImage(named: name))
            imgView.contentMode = .scaleAspectFill
            imgView.clipsToBounds = true
            imgView.layer.cornerRadius = 18
            imgView.layer.borderWidth = 2
            imgView.layer.borderColor = UIColor.white.cgColor
            imgView.translatesAutoresizingMaskIntoConstraints = false
            imgView.widthAnchor.constraint(equalToConstant: 36).isActive = true
            imgView.heightAnchor.constraint(equalToConstant: 36).isActive = true
            membersStack.addArrangedSubview(imgView)
        }
        refreshJoinState()
    }

@discardableResult
 func alwaysWhiteDirectoryAttributeTableView(hostMode: Bool) -> UITableView! {
    var galleryY: String! = String(cString: [114,97,110,115,102,111,114,109,0], encoding: .utf8)!
    var boxH: Double = 2.0
   repeat {
      boxH += Double(galleryY.count ^ 1)
      if 323539.0 == boxH {
         break
      }
   } while (3.83 <= (1.95 - boxH)) && (323539.0 == boxH)
     var cnewsDelta: Double = 51.0
     let removedPoint: Int = 92
     let startedPost: String! = String(cString: [111,98,106,101,99,116,115,0], encoding: .utf8)!
    var rtmpdhConsumer:UITableView! = UITableView(frame:CGRect(x: 276, y: 205, width: 0, height: 0))
    rtmpdhConsumer.delegate = nil
    rtmpdhConsumer.dataSource = nil
    rtmpdhConsumer.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rtmpdhConsumer.alpha = 0.8;
    rtmpdhConsumer.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rtmpdhConsumer.frame = CGRect(x: 290, y: 238, width: 0, height: 0)

    
    return rtmpdhConsumer

}






    private func setupUI() {

         var stampsIndata: UITableView! = alwaysWhiteDirectoryAttributeTableView(hostMode:false)

      if stampsIndata != nil {
          let stampsIndata_tag = stampsIndata.tag
          self.view.addSubview(stampsIndata)
      }

withUnsafeMutablePointer(to: &stampsIndata) { pointer in
    
}


       var cell0: [Any]! = [String(cString: [101,120,105,115,116,115,0], encoding: .utf8)!, String(cString: [100,105,116,104,101,114,0], encoding: .utf8)!, String(cString: [115,105,103,110,108,101,0], encoding: .utf8)!]
      cell0 = [cell0.count ^ 1]

        view.backgroundColor = .white
        bgView.contentMode = .scaleAspectFill
        bgView.clipsToBounds = true
        bgView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(bgView)

        header.configure(title: "", showBack: true, rightImage: "chatroom_alert")
        header.onBack = { [weak self] in self?.navigationController?.popViewController(animated: true) }
        header.onRight = { [weak self] in
            guard let self else { return }
            ReportSheetPresenter.present(from: self, targetUserId: self.team.hostId, postId: nil)
        }
        header.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(header)

        scrollView.translatesAutoresizingMaskIntoConstraints = false
        scrollView.alwaysBounceVertical = true
        contentView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(scrollView)
        scrollView.addSubview(contentView)

        avatarView.contentMode = .scaleAspectFill
        avatarView.clipsToBounds = true
        avatarView.layer.cornerRadius = 30
        avatarView.isUserInteractionEnabled = true
        avatarView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(openHost)))

        nameLabel.font = .systemFont(ofSize: 16, weight: .semibold)
        nameLabel.textColor = .black
        dateLabel.font = .systemFont(ofSize: 13, weight: .regular)
        dateLabel.textColor = AppTheme.secondaryText

        card.backgroundColor = .white
        card.layer.cornerRadius = 22
        card.clipsToBounds = true

        routeTitleLabel.text = "Route:"
        routeTitleLabel.font = .systemFont(ofSize: 18, weight: .bold)
        routeTitleLabel.textColor = .black

        routeValueLabel.font = .systemFont(ofSize: 15, weight: .regular)
        routeValueLabel.textColor = .black
        routeValueLabel.numberOfLines = 0

        descTitleLabel.text = "Route description:"
        descTitleLabel.font = .systemFont(ofSize: 18, weight: .bold)
        descTitleLabel.textColor = .black

        descValueLabel.font = .systemFont(ofSize: 14, weight: .regular)
        descValueLabel.textColor = .black
        descValueLabel.numberOfLines = 0

        membersStack.axis = .horizontal
        membersStack.spacing = -10
        membersStack.alignment = .center

        joinButton.layer.cornerRadius = 28
        joinButton.clipsToBounds = true
        joinButton.titleLabel?.font = .systemFont(ofSize: 18, weight: .semibold)
        joinButton.addTarget(self, action: #selector(joinTap), for: .touchUpInside)

        coinIcon.contentMode = .scaleAspectFit
        coinIcon.isUserInteractionEnabled = false

        [avatarView, nameLabel, dateLabel, card, joinButton, coinIcon].forEach {
            $0.translatesAutoresizingMaskIntoConstraints = false
        }
        contentView.addSubview(avatarView)
        contentView.addSubview(nameLabel)
        contentView.addSubview(dateLabel)
        contentView.addSubview(card)
        view.addSubview(joinButton)
        joinButton.addSubview(coinIcon)

        [routeTitleLabel, routeValueLabel, descTitleLabel, descValueLabel, membersStack].forEach {
            $0.translatesAutoresizingMaskIntoConstraints = false
            card.addSubview($0)
        }

        NSLayoutConstraint.activate([
            bgView.topAnchor.constraint(equalTo: view.topAnchor),
            bgView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            bgView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            bgView.bottomAnchor.constraint(equalTo: view.bottomAnchor),

            header.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor),
            header.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            header.trailingAnchor.constraint(equalTo: view.trailingAnchor),

            scrollView.topAnchor.constraint(equalTo: header.bottomAnchor),
            scrollView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            scrollView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            scrollView.bottomAnchor.constraint(equalTo: joinButton.topAnchor, constant: -12),

            contentView.topAnchor.constraint(equalTo: scrollView.topAnchor),
            contentView.leadingAnchor.constraint(equalTo: scrollView.leadingAnchor),
            contentView.trailingAnchor.constraint(equalTo: scrollView.trailingAnchor),
            contentView.bottomAnchor.constraint(equalTo: scrollView.bottomAnchor),
            contentView.widthAnchor.constraint(equalTo: scrollView.widthAnchor),

            avatarView.topAnchor.constraint(equalTo: contentView.topAnchor, constant: 18),
            avatarView.leadingAnchor.constraint(equalTo: contentView.leadingAnchor, constant: 24),
            avatarView.widthAnchor.constraint(equalToConstant: 60),
            avatarView.heightAnchor.constraint(equalToConstant: 60),

            nameLabel.leadingAnchor.constraint(equalTo: avatarView.trailingAnchor, constant: 12),
            nameLabel.topAnchor.constraint(equalTo: avatarView.topAnchor, constant: 10),
            nameLabel.trailingAnchor.constraint(equalTo: contentView.trailingAnchor, constant: -24),

            dateLabel.leadingAnchor.constraint(equalTo: nameLabel.leadingAnchor),
            dateLabel.topAnchor.constraint(equalTo: nameLabel.bottomAnchor, constant: 4),
            dateLabel.trailingAnchor.constraint(equalTo: nameLabel.trailingAnchor),

            card.topAnchor.constraint(equalTo: avatarView.bottomAnchor, constant: 20),
            card.leadingAnchor.constraint(equalTo: contentView.leadingAnchor, constant: 20),
            card.trailingAnchor.constraint(equalTo: contentView.trailingAnchor, constant: -20),
            card.bottomAnchor.constraint(equalTo: contentView.bottomAnchor, constant: -20),

            routeTitleLabel.topAnchor.constraint(equalTo: card.topAnchor, constant: 22),
            routeTitleLabel.leadingAnchor.constraint(equalTo: card.leadingAnchor, constant: 20),
            routeTitleLabel.trailingAnchor.constraint(equalTo: card.trailingAnchor, constant: -20),

            routeValueLabel.topAnchor.constraint(equalTo: routeTitleLabel.bottomAnchor, constant: 10),
            routeValueLabel.leadingAnchor.constraint(equalTo: routeTitleLabel.leadingAnchor),
            routeValueLabel.trailingAnchor.constraint(equalTo: routeTitleLabel.trailingAnchor),

            descTitleLabel.topAnchor.constraint(equalTo: routeValueLabel.bottomAnchor, constant: 22),
            descTitleLabel.leadingAnchor.constraint(equalTo: routeTitleLabel.leadingAnchor),
            descTitleLabel.trailingAnchor.constraint(equalTo: routeTitleLabel.trailingAnchor),

            descValueLabel.topAnchor.constraint(equalTo: descTitleLabel.bottomAnchor, constant: 10),
            descValueLabel.leadingAnchor.constraint(equalTo: routeTitleLabel.leadingAnchor),
            descValueLabel.trailingAnchor.constraint(equalTo: routeTitleLabel.trailingAnchor),

            membersStack.topAnchor.constraint(equalTo: descValueLabel.bottomAnchor, constant: 24),
            membersStack.leadingAnchor.constraint(equalTo: routeTitleLabel.leadingAnchor),
            membersStack.heightAnchor.constraint(equalToConstant: 36),
            membersStack.bottomAnchor.constraint(equalTo: card.bottomAnchor, constant: -22),

            joinButton.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 48),
            joinButton.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -48),
            joinButton.bottomAnchor.constraint(equalTo: view.safeAreaLayoutGuide.bottomAnchor, constant: -18),
            joinButton.heightAnchor.constraint(equalToConstant: 56),

            coinIcon.centerYAnchor.constraint(equalTo: joinButton.centerYAnchor),
            coinIcon.trailingAnchor.constraint(equalTo: joinButton.trailingAnchor, constant: -28),
            coinIcon.widthAnchor.constraint(equalToConstant: 28),
            coinIcon.heightAnchor.constraint(equalToConstant: 28)
        ])
    }

@discardableResult
 func unreadLightPrimaryStepMapString() -> Float {
    var starred3: String! = String(cString: [99,111,108,115,107,105,112,0], encoding: .utf8)!
    var avatarsZ: [String: Any]! = [String(cString: [119,114,105,116,101,116,114,117,110,99,0], encoding: .utf8)!:11, String(cString: [116,101,110,115,105,111,110,0], encoding: .utf8)!:44]
    var flagn: Float = 2.0
   for _ in 0 ..< 3 {
      avatarsZ[starred3] = 2 * starred3.count
   }
   while ((Int(flagn > 160728085.0 || flagn < -160728085.0 ? 29.0 : flagn) / (Swift.max(starred3.count, 4))) < 3 || 1 < (starred3.count << (Swift.min(labs(3), 3)))) {
      starred3.append("\((Int(flagn > 109403124.0 || flagn < -109403124.0 ? 24.0 : flagn) % (Swift.max(1, 4))))")
      break
   }
      avatarsZ = ["\(avatarsZ.values.count)": (Int(flagn > 14875797.0 || flagn < -14875797.0 ? 89.0 : flagn) << (Swift.min(avatarsZ.values.count, 4)))]
   return flagn

}






    private func routeDateText() -> String {

         let timstampInterpolator: Float = unreadLightPrimaryStepMapString()

      if timstampInterpolator >= 65 {
             print(timstampInterpolator)
      }

_ = timstampInterpolator


       var defaultsg: Double = 1.0
   if 5.68 < (Double(Double(3) + defaultsg)) {
      defaultsg /= Swift.max(5, Double(1))
   }

        
        let member = abs(team.id.hashValue % 20) + 1
        return String(format: "2026-%02d-%02d", (member % 12) + 1, member)
    }

@discardableResult
 func nowDeadlineUseStateTableView(purchaseEncoder: Float, buttonsTap: String!, badgeApple: Int) -> UITableView! {
    var willI: [Any]! = [13, 52]
    var stepP: String! = String(cString: [99,111,111,108,100,111,119,110,115,0], encoding: .utf8)!
       var ridersA: String! = String(cString: [118,105,100,115,116,97,98,117,116,105,108,115,0], encoding: .utf8)!
       _ = ridersA
       var more6: Double = 3.0
          var lowerS: Int = 2
         withUnsafeMutablePointer(to: &lowerS) { pointer in
    
         }
          var handleq: String! = String(cString: [117,110,115,112,105,108,108,0], encoding: .utf8)!
          var membersQ: Double = 4.0
          _ = membersQ
         more6 /= Swift.max(1, (Double((String(cString:[115,0], encoding: .utf8)!) == handleq ? handleq.count : Int(membersQ > 112923946.0 || membersQ < -112923946.0 ? 85.0 : membersQ))))
         lowerS /= Swift.max(3 ^ handleq.count, 2)
          var agree9: Float = 4.0
          var valueK: [String: Any]! = [String(cString: [103,98,114,112,0], encoding: .utf8)!:true]
         ridersA.append("\(2)")
         agree9 /= Swift.max(5, (Float(valueK.values.count * Int(agree9 > 223379519.0 || agree9 < -223379519.0 ? 21.0 : agree9))))
         valueK["\(more6)"] = (Int(agree9 > 224677294.0 || agree9 < -224677294.0 ? 83.0 : agree9))
      while (ridersA.hasPrefix("\(more6)")) {
          var route3: [Any]! = [10, 26]
          var intrinsicl: String! = String(cString: [103,105,103,97,98,121,116,101,115,0], encoding: .utf8)!
         ridersA.append("\(route3.count * 1)")
         intrinsicl = "\((Int(more6 > 349936128.0 || more6 < -349936128.0 ? 22.0 : more6) & route3.count))"
         break
      }
      if 2.36 >= more6 {
          var starsX: Double = 2.0
          var showsN: Int = 4
         withUnsafeMutablePointer(to: &showsN) { pointer in
                _ = pointer.pointee
         }
         more6 += (Double(Int(more6 > 13199585.0 || more6 < -13199585.0 ? 44.0 : more6)))
         starsX -= (Double(Int(more6 > 192810434.0 || more6 < -192810434.0 ? 52.0 : more6) | showsN))
         showsN /= Swift.max(3, (Int(more6 > 10790196.0 || more6 < -10790196.0 ? 80.0 : more6) / 3))
      }
          var route8: Int = 2
          _ = route8
          var authorz: String! = String(cString: [114,105,100,0], encoding: .utf8)!
          _ = authorz
         more6 += Double(ridersA.count)
         route8 -= ((String(cString:[121,0], encoding: .utf8)!) == authorz ? Int(more6 > 86535430.0 || more6 < -86535430.0 ? 79.0 : more6) : authorz.count)
      if 1 > (Int(more6 > 79322047.0 || more6 < -79322047.0 ? 37.0 : more6) / (Swift.max(ridersA.count, 3))) || 2.29 > (more6 / (Swift.max(2.19, 10))) {
          var root1: Int = 2
          var tapL: Float = 0.0
          var constraintR: [Any]! = [89, 5, 58]
          var postsR: Bool = true
          var editp: String! = String(cString: [115,104,97,114,112,101,110,0], encoding: .utf8)!
         ridersA = "\((3 / (Swift.max(5, (postsR ? 2 : 4)))))"
         root1 += (Int(tapL > 118610054.0 || tapL < -118610054.0 ? 76.0 : tapL))
         tapL -= (Float(Int(tapL > 149444181.0 || tapL < -149444181.0 ? 45.0 : tapL)))
         constraintR = [((postsR ? 4 : 5) + Int(tapL > 390193592.0 || tapL < -390193592.0 ? 40.0 : tapL))]
         editp = "\((Int(more6 > 261428502.0 || more6 < -261428502.0 ? 86.0 : more6) >> (Swift.min(labs(2), 2))))"
      }
      willI = [1]
      stepP.append("\(willI.count)")
     let userCountries: Double = 21.0
     var heroEncoder: Bool = true
     let handleFriend_4h: Int = 13
     var clearanceNext: [Any]! = [73, 34, 72]
    var specifiedStripTop:UITableView! = UITableView(frame:CGRect.zero)
    specifiedStripTop.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    specifiedStripTop.delegate = nil
    specifiedStripTop.dataSource = nil
    specifiedStripTop.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    specifiedStripTop.alpha = 0.6
    specifiedStripTop.frame = CGRect(x: 108, y: 307, width: 0, height: 0)

    
    return specifiedStripTop

}






    @objc private func openHost() {

         var chsetRequested: UITableView! = nowDeadlineUseStateTableView(purchaseEncoder:25.0, buttonsTap:String(cString: [106,97,99,111,98,105,97,110,0], encoding: .utf8)!, badgeApple:33)

      if chsetRequested != nil {
          self.view.addSubview(chsetRequested)
          let chsetRequested_tag = chsetRequested.tag
      }

withUnsafeMutablePointer(to: &chsetRequested) { pointer in
        _ = pointer.pointee
}


       var avatarsh: String! = String(cString: [112,111,115,108,105,115,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &avatarsh) { pointer in
    
   }
    var follow3: String! = String(cString: [118,97,97,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!
    _ = follow3
   while (4 == avatarsh.count) {
      follow3.append("\(1)")
      break
   }
      avatarsh.append("\(2 * avatarsh.count)")

        pushFromTab(MMO_TeamThemeController(userId: team.hostId))
    }


    private func refreshJoinState() {
       var topicx: [String: Any]! = [String(cString: [115,119,102,112,108,97,121,101,114,0], encoding: .utf8)!:7.0]
   withUnsafeMutablePointer(to: &topicx) { pointer in
    
   }
      topicx = ["\(topicx.keys.count)": topicx.count + 2]

        let window_e9 = MMO_MotoLaunch.shared.teams().first(where: { $0.id == team.id })
        if let window_e9 { team = window_e9 }
        if team.joined {
            joinButton.backgroundColor = UIColor(white: 0.72, alpha: 1)
            joinButton.setTitle("Joined", for: .normal)
            joinButton.setTitleColor(.white, for: .normal)
            joinButton.isEnabled = false
            coinIcon.isHidden = true
        } else {
            joinButton.backgroundColor = .black
            joinButton.setTitle("Join", for: .normal)
            joinButton.setTitleColor(.white, for: .normal)
            joinButton.isEnabled = true
            coinIcon.isHidden = false
        }
    }


    override func viewDidLoad() {
       var removedp: String! = String(cString: [117,110,99,111,100,101,100,0], encoding: .utf8)!
    _ = removedp
      removedp.append("\(removedp.count ^ removedp.count)")

        super.viewDidLoad()
        navigationController?.setNavigationBarHidden(true, animated: false)
        setupUI()
        bindData()
    }
}
