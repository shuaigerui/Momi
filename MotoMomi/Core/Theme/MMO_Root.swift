
import Foundation

import UIKit

enum AppTheme {
    static let accent = UIColor(red: 52/255, green: 255/255, blue: 193/255, alpha: 1)
    static let accentDeep = UIColor(red: 20/255, green: 220/255, blue: 180/255, alpha: 1)
    static let ink = UIColor(red: 18/255, green: 18/255, blue: 18/255, alpha: 1)
    static let secondaryText = UIColor(red: 120/255, green: 120/255, blue: 128/255, alpha: 1)
    static let cardBorder = UIColor.black.withAlphaComponent(0.85)
    static let softMint = UIColor(red: 180/255, green: 245/255, blue: 230/255, alpha: 1)
    static let topMint = UIColor(red: 120/255, green: 235/255, blue: 220/255, alpha: 1)
    static let pageBottom = UIColor(red: 245/255, green: 255/255, blue: 250/255, alpha: 1)

    static func gradientColors() -> [CGColor] {
       var durationD: Double = 0.0
      durationD -= (Double(Int(durationD > 271051871.0 || durationD < -271051871.0 ? 71.0 : durationD) >> (Swift.min(1, labs(1)))))

return         [
            topMint.cgColor,
            UIColor.white.cgColor,
            softMint.cgColor
        ]
    }

    static func primaryGradientLayer(frame: CGRect) -> CAGradientLayer {
       var formatterD: Double = 2.0
    var resultJ: Double = 1.0
   while (4.85 == (formatterD + resultJ)) {
      resultJ -= (Double(Int(formatterD > 103060837.0 || formatterD < -103060837.0 ? 40.0 : formatterD) | 2))
      break
   }

   repeat {
      resultJ -= (Double(Int(formatterD > 270678811.0 || formatterD < -270678811.0 ? 67.0 : formatterD)))
      if 316567.0 == resultJ {
         break
      }
   } while (316567.0 == resultJ) && ((resultJ - formatterD) >= 4.32 && (resultJ - formatterD) >= 4.32)
        let users = CAGradientLayer()
        users.colors = [accent.cgColor, accentDeep.cgColor]
        users.startPoint = CGPoint(x: 0, y: 0.5)
        users.endPoint = CGPoint(x: 1, y: 0.5)
        users.frame = frame
        users.cornerRadius = frame.height / 2
        return users
    }
}

enum LayoutMetric {
    static var screenWidth: CGFloat {
       var sectionz: String! = String(cString: [115,117,109,100,105,102,102,0], encoding: .utf8)!
   while (!sectionz.hasSuffix(sectionz)) {
      sectionz.append("\(sectionz.count)")
      break
   }
     return UIScreen.main.bounds.width }
    static var screenHeight: CGFloat {
       var dots9: String! = String(cString: [97,112,110,115,0], encoding: .utf8)!
    var stats2: String! = String(cString: [117,110,98,105,110,100,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      stats2.append("\(dots9.count)")
   }
      dots9 = "\(dots9.count)"
     return UIScreen.main.bounds.height }
    static var isCompactHeight: Bool {
       var lastA: String! = String(cString: [99,111,110,115,111,108,101,0], encoding: .utf8)!
    var usero: String! = String(cString: [112,114,101,0], encoding: .utf8)!
    _ = usero
   if lastA.count == 4 {
       var textsP: Double = 0.0
      repeat {
         textsP /= Swift.max((Double(Int(textsP > 3665125.0 || textsP < -3665125.0 ? 22.0 : textsP) >> (Swift.min(2, labs(2))))), 1)
         if textsP == 3837728.0 {
            break
         }
      } while (textsP == 3837728.0) && (1.20 <= (textsP - textsP) && (textsP - textsP) <= 1.20)
       var conversation7: String! = String(cString: [112,104,121,115,0], encoding: .utf8)!
       var register_aX: String! = String(cString: [102,97,108,115,101,0], encoding: .utf8)!
       _ = register_aX
         register_aX = "\((Int(textsP > 280311235.0 || textsP < -280311235.0 ? 11.0 : textsP) ^ 3))"
         conversation7.append("\(3 | register_aX.count)")
      usero = "\((usero.count + Int(textsP > 358979640.0 || textsP < -358979640.0 ? 31.0 : textsP)))"
   }
      usero.append("\(2)")
      lastA = "\(lastA.count + lastA.count)"
     return screenHeight < 700 }
    static var safeTop: CGFloat {
       var polylineL: Int = 0
    var b_imageu: Double = 3.0
      polylineL += (Int(b_imageu > 28537494.0 || b_imageu < -28537494.0 ? 79.0 : b_imageu))

            return UIApplication.shared.connectedScenes
            .compactMap { $0 as? UIWindowScene }
            .flatMap { $0.windows }
            .first { $0.isKeyWindow }?
            .safeAreaInsets.top ?? 47
       var bikeA: String! = String(cString: [100,101,99,111,100,101,102,114,97,109,101,0], encoding: .utf8)!
       var bind8: [Any]! = [24, 95, 54]
       var insetsF: [String: Any]! = [String(cString: [99,115,119,97,112,0], encoding: .utf8)!:70, String(cString: [106,97,99,107,0], encoding: .utf8)!:0]
      repeat {
          var deltab: Bool = true
          _ = deltab
          var short_7N: Int = 5
          var previousN: Float = 5.0
          var heroP: String! = String(cString: [109,110,101,109,111,110,105,99,0], encoding: .utf8)!
         bind8 = [bind8.count]
         deltab = (bind8.contains { $0 as? Float == previousN })
         short_7N %= Swift.max(3, (bind8.count << (Swift.min(4, labs(Int(previousN > 159153996.0 || previousN < -159153996.0 ? 37.0 : previousN))))))
         heroP.append("\(2)")
         if 3200182 == bind8.count {
            break
         }
      } while (insetsF["\(bind8.count)"] != nil) && (3200182 == bind8.count)
      while (!bikeA.hasSuffix("\(bind8.count)")) {
          var mutualH: String! = String(cString: [99,111,111,114,100,105,110,97,116,101,115,0], encoding: .utf8)!
          var pathD: String! = String(cString: [100,111,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pathD) { pointer in
                _ = pointer.pointee
         }
          var peerp: Double = 5.0
          var clientZ: Float = 3.0
          var completed8: String! = String(cString: [117,110,115,101,110,100,0], encoding: .utf8)!
         bind8 = [(Int(clientZ > 176445437.0 || clientZ < -176445437.0 ? 74.0 : clientZ) & 3)]
         mutualH.append("\((mutualH == (String(cString:[80,0], encoding: .utf8)!) ? mutualH.count : Int(clientZ > 337193734.0 || clientZ < -337193734.0 ? 28.0 : clientZ)))")
         pathD.append("\(bikeA.count + bind8.count)")
         peerp -= Double(insetsF.values.count % 3)
         completed8.append("\(insetsF.values.count / (Swift.max(mutualH.count, 2)))")
         break
      }
         bind8 = [3 >> (Swift.min(2, bind8.count))]
      if insetsF["\(bind8.count)"] != nil {
          var lengthA: Float = 0.0
          var container8: Double = 4.0
          var posts_: String! = String(cString: [108,111,99,107,99,104,97,105,110,0], encoding: .utf8)!
          _ = posts_
         bind8.append(posts_.count)
         lengthA += Float(bind8.count % 1)
         container8 += Double(posts_.count)
      }
         bikeA.append("\(bind8.count % (Swift.max(bikeA.count, 3)))")
      b_imageu += Double(bikeA.count ^ 3)
    }
    static var safeBottom: CGFloat {
       var motou: [Any]! = [99, 12, 30]
   withUnsafeMutablePointer(to: &motou) { pointer in
    
   }
       var followM: String! = String(cString: [111,114,100,101,114,0], encoding: .utf8)!
       var chath: String! = String(cString: [114,101,99,111,109,112,117,116,101,0], encoding: .utf8)!
         followM = "\(followM.count)"
         followM.append("\(chath.count)")
      while (3 > chath.count || 3 > followM.count) {
         chath = "\(chath.count / (Swift.max(1, followM.count)))"
         break
      }
         followM = "\((followM == (String(cString:[100,0], encoding: .utf8)!) ? chath.count : followM.count))"
       var v_animationp: Int = 5
         v_animationp >>= Swift.min(labs(2), 2)
      motou = [((String(cString:[51,0], encoding: .utf8)!) == chath ? followM.count : chath.count)]

            return UIApplication.shared.connectedScenes
            .compactMap { $0 as? UIWindowScene }
            .flatMap { $0.windows }
            .first { $0.isKeyWindow }?
            .safeAreaInsets.bottom ?? 34
    }
    static let tabBarHeight: CGFloat = 64
    static let coinCost: Int = 10
}

extension UIColor {
    convenience init(hex: String) {
        var s = hex.trimmingCharacters(in: .whitespacesAndNewlines).uppercased()
        if s.hasPrefix("#") { s.removeFirst() }
        var rgb: UInt64 = 0
        Scanner(string: s).scanHexInt64(&rgb)
        self.init(
            red: CGFloat((rgb & 0xFF0000) >> 16) / 255,
            green: CGFloat((rgb & 0x00FF00) >> 8) / 255,
            blue: CGFloat(rgb & 0x0000FF) / 255,
            alpha: 1
        )
    }
}

extension UIImage {

    static func named(_ name: String) -> UIImage? {
       var json_: Int = 1
   withUnsafeMutablePointer(to: &json_) { pointer in
          _ = pointer.pointee
   }
       var purgez: String! = String(cString: [97,115,115,101,109,98,108,101,114,0], encoding: .utf8)!
      while (purgez == purgez) {
          var section9: String! = String(cString: [112,97,114,97,109,0], encoding: .utf8)!
         purgez = "\(2)"
         section9.append("\(section9.count / 3)")
         break
      }
      if purgez.hasSuffix("\(purgez.count)") {
         purgez = "\(3 / (Swift.max(7, purgez.count)))"
      }
          var bundleq: Double = 0.0
          var clienta: Float = 1.0
         purgez = "\((Int(bundleq > 214593528.0 || bundleq < -214593528.0 ? 4.0 : bundleq) ^ Int(clienta > 380065347.0 || clienta < -380065347.0 ? 2.0 : clienta)))"
      json_ ^= ((String(cString:[87,0], encoding: .utf8)!) == purgez ? purgez.count : json_)

return         UIImage(named: name)
    }
}

extension UIView {

@discardableResult
 func countryStateParentCost(auto_gFollowers: [Any]!, conversationsObserver: Double, conversationsPicker: Int) -> Int {
    var r_managerm: String! = String(cString: [99,111,109,112,111,115,101,100,0], encoding: .utf8)!
    var rider3: [String: Any]! = [String(cString: [101,120,112,111,110,101,110,116,105,97,116,105,111,110,0], encoding: .utf8)!:41, String(cString: [105,115,115,101,116,117,103,105,100,0], encoding: .utf8)!:76]
    var loado: Int = 0
      r_managerm.append("\(2)")
   for _ in 0 ..< 2 {
      rider3["\(loado)"] = 1
   }
   repeat {
       var metersv: Int = 4
       var named: String! = String(cString: [99,97,112,105,0], encoding: .utf8)!
       var frame_61: String! = String(cString: [101,108,108,105,103,105,98,108,101,0], encoding: .utf8)!
       _ = frame_61
       var followingJ: Int = 4
          var motok: String! = String(cString: [108,117,116,100,0], encoding: .utf8)!
          var navigationv: Double = 3.0
         followingJ += 2
         motok.append("\(1)")
         navigationv += Double(3 / (Swift.max(8, metersv)))
      repeat {
         frame_61.append("\(metersv % 1)")
         if frame_61 == (String(cString:[52,117,51,0], encoding: .utf8)!) {
            break
         }
      } while (frame_61.contains("\(followingJ)")) && (frame_61 == (String(cString:[52,117,51,0], encoding: .utf8)!))
      for _ in 0 ..< 3 {
         frame_61 = "\(2)"
      }
      for _ in 0 ..< 2 {
          var signO: Bool = false
          var alln: [String: Any]! = [String(cString: [98,111,117,110,100,101,100,0], encoding: .utf8)!:19.0]
          var srca: Float = 5.0
          var polylinei: String! = String(cString: [115,105,108,101,110,99,101,0], encoding: .utf8)!
          _ = polylinei
          var captionA: String! = String(cString: [97,118,99,111,100,101,99,114,101,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &captionA) { pointer in
    
         }
         followingJ ^= captionA.count
         signO = nil == alln["\(metersv)"]
         alln = ["\(signO)": (Int(srca > 148220677.0 || srca < -148220677.0 ? 87.0 : srca) - 2)]
         srca -= Float(metersv + 2)
         polylinei.append("\(((String(cString:[114,0], encoding: .utf8)!) == named ? Int(srca > 138234316.0 || srca < -138234316.0 ? 14.0 : srca) : named.count))")
      }
      for _ in 0 ..< 1 {
          var applicationS: String! = String(cString: [119,101,98,114,116,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &applicationS) { pointer in
    
         }
          var following7: String! = String(cString: [115,117,98,116,114,97,99,116,111,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &following7) { pointer in
    
         }
          var groupF: Float = 4.0
          var popg: Double = 2.0
         withUnsafeMutablePointer(to: &popg) { pointer in
    
         }
         metersv -= applicationS.count
         following7.append("\(followingJ)")
         groupF /= Swift.max(3, Float(3 >> (Swift.min(5, applicationS.count))))
         popg += Double(metersv & frame_61.count)
      }
         named.append("\(((String(cString:[75,0], encoding: .utf8)!) == frame_61 ? frame_61.count : followingJ))")
      repeat {
          var backF: Bool = false
         withUnsafeMutablePointer(to: &backF) { pointer in
                _ = pointer.pointee
         }
          var bootstrappedh: Bool = false
          var circle1: String! = String(cString: [102,114,101,101,112,97,100,100,114,115,0], encoding: .utf8)!
          var seconds4: [Any]! = [43, 57]
          var membery: Float = 1.0
         named = "\(1 % (Swift.max(9, seconds4.count)))"
         backF = !backF
         bootstrappedh = (circle1.count / (Swift.max(9, seconds4.count))) > 31
         circle1 = "\(circle1.count - 3)"
         membery -= Float(2)
         if named.count == 1198030 {
            break
         }
      } while ((2 & named.count) == 5 && 1 == (2 & metersv)) && (named.count == 1198030)
         frame_61 = "\(3 * followingJ)"
       var rendererm: Double = 3.0
         rendererm -= Double(2)
          var rootM: String! = String(cString: [105,115,114,101,97,100,111,110,108,121,0], encoding: .utf8)!
         named.append("\(metersv + 1)")
         rootM.append("\((2 + Int(rendererm > 149609315.0 || rendererm < -149609315.0 ? 62.0 : rendererm)))")
         frame_61.append("\((Int(rendererm > 152479469.0 || rendererm < -152479469.0 ? 65.0 : rendererm)))")
      r_managerm.append("\(metersv)")
      if r_managerm.count == 1061269 {
         break
      }
   } while (1 >= (loado << (Swift.min(labs(2), 3))) || 4 >= (2 << (Swift.min(3, r_managerm.count)))) && (r_managerm.count == 1061269)
   return loado

}





    func pinEdges(to view: UIView, insets: UIEdgeInsets = .zero) {

         var demuxWritable: Int = countryStateParentCost(auto_gFollowers:[93, 59, 91], conversationsObserver:54.0, conversationsPicker:5)

      print(demuxWritable)

withUnsafeMutablePointer(to: &demuxWritable) { pointer in
    
}


       var site2: Double = 4.0
   withUnsafeMutablePointer(to: &site2) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 1 {
       var hero9: String! = String(cString: [112,111,108,121,107,101,121,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         hero9 = "\(hero9.count)"
      }
      for _ in 0 ..< 1 {
         hero9 = "\(1)"
      }
      if hero9 != String(cString:[71,0], encoding: .utf8)! {
          var formatterM: [Any]! = [96, 97]
          var groupT: String! = String(cString: [112,104,111,110,101,0], encoding: .utf8)!
          _ = groupT
          var servicesa: Double = 3.0
          _ = servicesa
          var g_height3: String! = String(cString: [110,101,97,114,108,121,0], encoding: .utf8)!
          var collectionN: Int = 2
          _ = collectionN
         hero9 = "\(groupT.count)"
         formatterM.append((Int(servicesa > 25494704.0 || servicesa < -25494704.0 ? 55.0 : servicesa) % (Swift.max(hero9.count, 1))))
         servicesa += Double(collectionN & 1)
         g_height3 = "\(((String(cString:[50,0], encoding: .utf8)!) == groupT ? groupT.count : g_height3.count))"
         collectionN /= Swift.max(1, 1)
      }
      site2 += (Double(1 >> (Swift.min(4, labs(Int(site2 > 143122503.0 || site2 < -143122503.0 ? 56.0 : site2))))))
   }

        translatesAutoresizingMaskIntoConstraints = false
        NSLayoutConstraint.activate([
            topAnchor.constraint(equalTo: view.topAnchor, constant: insets.top),
            leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: insets.left),
            trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -insets.right),
            bottomAnchor.constraint(equalTo: view.bottomAnchor, constant: -insets.bottom)
        ])
    }


    func applyCardBorder(radius: CGFloat = 18) {
       var itemn: String! = String(cString: [110,100,101,102,0], encoding: .utf8)!
      itemn.append("\(itemn.count ^ 2)")

        layer.cornerRadius = radius
        layer.borderWidth = 1.5
        layer.borderColor = UIColor.black.cgColor
        clipsToBounds = true
    }
}
